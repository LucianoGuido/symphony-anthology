# {{PROJECT_NAME}}

This project was generated with the local `create-symphony-anthology` preview.

## Start here

1. Open `index.html` directly in a browser, or serve the folder with a simple static server.
2. Edit `index.html` using semantic HTML first.
3. Keep layout primitives optional and use `data-*` variants only when intent matters.

## Files

- `styles/symphony.core.css`: copied from the current Anthology build
- `styles/site.css`: your local starter surface
